# AI-Powered Personal Finance Manager — Project Scaffold

## Temporary

- Create backend/package.json with dependencies: express, pg, bcrypt, jsonwebtoken, dotenv, cors, multer (if needed).

- Create backend/src/index.js (App entry) with REST endpoints and DB pool. The canvas includes routes files outlined above.

The backend will also call the ML service (FastAPI) for analysis via HTTP request.
